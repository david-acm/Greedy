@using System.Drawing
<DragSurface>
  @for (var index = 1; index <= DiceValues.Count; index++)
  {
    var value = DiceValues[index - 1];
    <Draggable InitialPosition="@((180 * (index - 1), 0))">
      <Die @key="index" DiceValue=@value Size="50"></Die>
    </Draggable>
  }
</DragSurface>